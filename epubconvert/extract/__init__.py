"""
Getting the reader's data off the Mac.

Apple's two SQLite databases, the package documents in the library, and the
checks that say whether a book can be converted at all. This layer decides
what a book *is*. It never decides how to write one down.
"""
