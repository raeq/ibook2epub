"""
Writing files out.

The epub archives, the catalogue in either shape, the vault of notes, the
detached annotation export, and the naming policies that decide what each is
called. Reads :mod:`epubconvert.collect`, and is read by
:mod:`epubconvert.run`.
"""
