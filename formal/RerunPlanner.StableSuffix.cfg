\* The same under --on-collision suffix, with digest markers and a title that looks like a suffix. Expected: both properties hold.
CONSTANTS
    N = 3
    Wanted <- SuffixLookalike
    Usable = {1, 2}
    OnCollision = "suffix"
    AllowMatch = FALSE
    AllowRefresh = TRUE
    AllowChanges = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK ExportedMeansTheBooksOwnFile NeverWritesOverAnotherBook
