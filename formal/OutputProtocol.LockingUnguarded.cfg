\* flock works for every run, no age guard: the code before the fix. Expected: every property holds; the guard is not needed here.
CONSTANTS
    Runs = {r1, r2, r3}
    Books = {b1, b2}
    LockWorks = {r1, r2, r3}
    NoRun = NoRun
    AgeGuard = FALSE
    LiveMayStall = FALSE
    MaxKills = 2
SPECIFICATION Spec
INVARIANTS TypeOK OnlyCompleteBooks NoLiveWorkSwept MutualExclusion
PROPERTIES AbandonedCleanedUp
