\* The same under --on-collision suffix with every identifier usable. Expected: violated; a book alone in a run is not crowded, so it gets no digest marker. Open defect.
CONSTANTS
    N = 3
    Wanted <- OneTitle
    Usable = {1, 2, 3}
    OnCollision = "suffix"
    AllowMatch = TRUE
    AllowRefresh = FALSE
    AllowChanges = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK ExportedMeansTheBooksOwnFile
