\* The same with --refresh. Expected: violated; a newer source is written over the removed edition's archive, which may be its only copy. Open defect.
CONSTANTS
    N = 3
    Wanted <- OneTitle
    Usable = {}
    OnCollision = "skip"
    AllowMatch = FALSE
    AllowRefresh = TRUE
    AllowChanges = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK NeverWritesOverAnotherBook
