\* Books added to and removed from the library between runs. Expected: violated. With the edition that holds the name removed, the next edition takes the name and is reported exported by the removed one's archive. Open defect.
CONSTANTS
    N = 3
    Wanted <- OneTitle
    Usable = {}
    OnCollision = "skip"
    AllowMatch = FALSE
    AllowRefresh = FALSE
    AllowChanges = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK ExportedMeansTheBooksOwnFile
