\* A fixed library, whole-library runs, --refresh allowed. Expected: both properties hold; the name on the shelf is a sound record here.
CONSTANTS
    N = 3
    Wanted <- OneTitle
    Usable = {}
    OnCollision = "skip"
    AllowMatch = FALSE
    AllowRefresh = TRUE
    AllowChanges = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK ExportedMeansTheBooksOwnFile NeverWritesOverAnotherBook
